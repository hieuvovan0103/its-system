# --- Server Configuration ---
server.port=8081
spring.application.name=assessment-service

# --- Database Configuration ---
# L?u ý: Dùng 'localhost' ?? ch?y code trên máy tính (k?t n?i vào port 5432 c?a Docker)
spring.datasource.url=jdbc:postgresql://localhost:5433/assessment_db
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# (Tùy ch?n) N?u b?n mu?n gi? schema riêng nh? bài tr??c, n?u không nó s? dùng schema 'public' m?c ??nh
# spring.jpa.properties.hibernate.default_schema=assessment_schema